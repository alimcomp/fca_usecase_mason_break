import 'dart:io';

import 'package:mason/mason.dart';

void run(HookContext context) async {
  // var progress = context.logger.progress('Installing packages');
  // await Process.run('flutter', ['packages', 'get']);

  // progress.complete();
  // progress = context.logger.progress('Running build runner');
  // await Process.run('flutter', ['pub', 'run', 'build_runner', 'build']);
  // progress.complete();
}
//flutter pub run build_runner build  --delete-conflicting-outputs